# Protected Truths

These are regression-only for CORR02 and must not be redesigned:
- Balanced6 exact-six source/provenance and `32/32` focused acceptance;
- 6 documents / 6 baseline revisions / 6 FTS rows idempotency;
- SQLite schema-v1, `node:sqlite`, persistence/provider boundary;
- Explicit Save != Autosave != Recovery; stale-save atomic rejection;
- Node `22.16.0` non-browser regression baseline;
- xterm/shared terminal runtime/provider separation;
- current package dependency graph;
- W03 workspace-first interaction; W04 evidence/review/decision/mastery domain boundaries; W05 truth ceilings;
- browser genuine-route status remains OPEN_ENVIRONMENT unless truly reproduced.
