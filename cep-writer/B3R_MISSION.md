# CEP CORR02 B3-R — Research & Quality + Visualize Truth Convergence

**Writer class:** `CHATGPT_WRITER`  
**Mode:** `MUTATING_CANDIDATE_ONLY__LOCAL_FIRST__NO_SELF_PROMOTION__NO_MAIN_MERGE__NO_RELEASE`  
**Exact accepted Product parent:** `fec137df4b06111db160cdcbd25d7c725dfec286`  
**Accepted Product source:** `e3951754ae79016603456b14dc71856671428e0d0811ec119388fbd79093890c / 273 files`  
**Transport branch:** `capsule/corr02-b3r-rq-visualize`

## Objective
Close only `F-027`, `F-034`, `F-035`, `F-036` plus the minimum RQ/Visualize provider, canonical-vs-representation and stale-harness truth required by those findings.

Affected Surfaces:
- Research & Quality (`rq`)
- Visualize (`TREE / PATH / GRAPH / CANVAS` as one Spatial workbench)

Consume accepted C1 shared carrier/region/contextual-toolbar mechanics, accepted C2 W04 truth, and accepted C3 Today truth. Do not duplicate or rewrite those owners.

## RQ truth requirements — F-027
1. Normal RQ Product must not present `BALANCED6_RQ_RECORDS` or any `LOCAL_DEV_ACCEPTANCE_SEED__DETERMINISTIC__RESETTABLE__NON_PRODUCTION` corpus as current canonical Product/provider truth.
2. RQ remains an Analytical knowledge-quality workbench using the accepted `AnalyticalCompare` owner. It is not Formal Evidence Review, does not issue Review Decisions, and never writes Mastery.
3. RQ profile remains `CONTRACT_ONLY / proof_consumer=false`; no candidate/reference packet or acceptance seed may be promoted into canonical runtime truth.
4. `rq.compare` is available only when an exact valid `SourceRevision` pair exists. If no admitted current RQ provider exists, use truthful unavailable/empty/non-production classification rather than inventing sources.
5. Search/provenance/working-analysis copy and availability must remain source/revision/provider bound. Durable working-analysis Save stays unavailable unless a governed persistence provider exists.

## Visualize truth requirements — F-034 / F-035
1. The current bound Balanced6 Visualize provider is `LOCAL_ACCEPTANCE_PROJECTION_ONLY` and returns `canonical:false`. Product copy/inspector/status must never call those relations or objects canonical.
2. Representation state is not canonical truth: `representationId != canonicalObjectId`; Canvas geometry, representation move/duplicate/remove, and Canvas-only links must not silently mutate canonical objects or relations.
3. Visualize remains one Spatial workbench using one shared Spatial interaction engine with Tree/Path/Graph/Canvas adapters. Do not create per-view engines or a second relation owner.
4. The current provider is read-only while the current SurfaceProfile declares domain-valid editing commands. First diagnose whether an already-governed editable provider/domain boundary exists in exact current source. Bind it only if its authority and canonical mutation contract are proven.
5. If no admitted editable provider exists, keep connect/edit/commit/undo/redo truthfully `UNAVAILABLE / READ_ONLY`; do **not** make the local acceptance provider writable/canonical to satisfy the Profile or old tests. If Product cannot reconcile availability without changing governing Profile semantics, STOP with the exact authority contradiction rather than mutating the profile.
6. Tree is the page-level `OWNER_CONFIRMED_FINAL_REFERENCE`. Path/Graph are supporting component references; Canvas is an `OWNER_CONFIRMED_SUPPORTING_COMPONENT_REFERENCE`. Supporting references do not replace Tree/shared-shell authority.

## F-036 stale-browser-harness correction
The three legacy relation flows currently assume an editable eligible relation pair against a read-only provider. `tools/browser-conformance.mjs` may be changed only to:
- test current read-only/provider availability truth on the normal Product path;
- exercise editing only against a clearly authorized test/provider harness whose semantics actually permit editing;
- preserve central reuse/one-owner assertions without forcing Product to fabricate editability.
Do not weaken valid accessibility, route, selection, spatial reuse or canonical-vs-representation assertions merely to turn the harness green.

## Writable Product allowlist
- `stack/native-typescript/adapters/rq/**`
- `stack/native-typescript/surfaces/rq/**`
- `stack/native-typescript/adapters/visualize/**`
- `stack/native-typescript/surfaces/visualize/**`
- `stack/native-typescript/surfaces/m0-controller-composition.ts`
- exact F-036 harness file `tools/browser-conformance.mjs`
- bounded B3-R harness under `tools/b3r-rq-visualize/**`
- bounded handoff under `writer-output/presentation-corr02-b3r/**`

Everything else is READ ONLY unless a directly reproduced prerequisite correctness defect satisfies OD-070 and requires Controller adjudication before scope widening.

## Explicitly prohibited
- Foundation/shared pane/toolbar/context/Spatial/Analytical owner rewrites or duplicate owners;
- Today/W04/W03/W05 Product correction;
- persistence/SQLite/runtime/terminal architecture or dependency changes;
- turning Balanced6/local acceptance/demo data into canonical Product truth;
- making the read-only Visualize acceptance provider writable merely to satisfy stale tests;
- mutating governing SurfaceProfiles/Owner decisions/oracles;
- RQ formal Review Decision/Mastery semantics;
- canonical object/relation mutation from representation-only operations;
- `cep-writer/**` mutation by the Writer;
- main merge/push, release, deployment, self-promotion.

## Required local-first execution
Use the Capsule as the only inbound workspace. Start from the prebuilt RQ + Visualize exact-parent Visual Bootstrap:
`verify -> materialize -> inspect RQ/Visualize baselines and governed references -> diagnose -> narrow fix -> local build/test/browser -> fresh recapture all material states/views -> compare -> falsify -> bounded handoff`.

The bootstrap is acceleration only. It does not prove genuine HTTP route/Back/Forward/network integration. Capture fresh post-mutation evidence locally and open every material screenshot.

## Positive / negative falsification gates
- normal RQ does not expose Balanced6/non-production acceptance records as current canonical provider truth;
- RQ compare requires an exact valid SourceRevision pair and provenance remains exact-revision bound;
- RQ does not gain W04 Review/Mastery authority or durable Save by implication;
- normal Visualize local acceptance projection never displays canonical copy/claims;
- `canonical:false` remains visible/truthful where the local projection is used;
- representation-only move/duplicate/remove/link operations do not mutate canonical domain truth;
- all four views remain one shared Spatial engine with distinct view semantics and no duplicate owner;
- read-only provider keeps connect/edit/commit/undo/redo unavailable unless an admitted editable provider is actually bound;
- an authorized editable harness/provider, if used for tests, is visibly/test-only classified and cannot leak into normal Product truth;
- F-036 browser harness no longer assumes editability from the read-only provider and still proves central reuse/selection/route behavior;
- Tree/Path/Graph/Canvas states are visually compared against their governed reference ceilings at `1440×1000` and `1024×900` as applicable;
- RQ is compared against its `REVIEWED_FINAL_CANDIDATE` as bounded Presentation input only;
- no dependency drift; `npm test`, `npm run build:runtime`, `npm run runtime:check` remain clean;
- rerun `npm run browser:test` / `npm run check` and classify any remaining failure as Product vs harness/evidence/environment before mutation.

## Required final handoff
Report exact parent, candidate HEAD/tree, Product source identity, changed Product/harness paths, finding-by-finding disposition, RQ provider classification, Visualize provider/canonical/editability truth, F-036 harness correction proof, model/build/runtime/browser/check results, visual comparison disposition, and exact final artifact identities.

## Stop Gate

## Final custody / output destination
- Heavy final CHATGPT_WRITER custody folder: `/Google Drive/cep_building_mgm/00_CONTROLLER/B3R_RQ_VISUALIZE_TRUTH_CONVERGENCE`
- Exact Google Drive folder ID: `1mOvbcrCNBi2iHnpOiyOWAiqUHZCZvRus`
- Final candidate/evidence packages, if materially required, must be written only to this folder with exact filename, bytes, SHA-256, final candidate HEAD/tree and Product-source identity recorded in the bounded handoff.
- Small textual receipts may also be committed only under `writer-output/presentation-corr02-b3r/**`.
- Intermediate screenshots/logs/diffs remain local/ephemeral under OD-073; do not churn Drive or GitHub with intermediate evidence.
- No output placement or upload creates acceptance, promotion, merge, release, deployment or stack-freeze authority.

End exactly:
`B3R_CANDIDATE_ONLY__CONTROLLER_AUDIT_REQUIRED`
