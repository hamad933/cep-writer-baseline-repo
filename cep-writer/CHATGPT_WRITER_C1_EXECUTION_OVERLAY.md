# CEP CORR02 — C1 CHATGPT WRITER EXECUTION OVERLAY

## Identity
- repository: `hamad933/cep-writer-baseline-repo`
- exact branch: `writer/presentation-corr02-c1-chatgpt`
- exact Product parent commit: `da007840363d92cdadd9f7c5180449a49e6f1d19`
- parent canonical source tree: `777d8b24032891b00bf3b61858a3be015b0b77fea710e1d5bd446d182577bb51 / 273 files`
- C1: `SERIAL_SHARED_OWNER_CORRECTION__CANDIDATE_ONLY__NO_SELF_PROMOTION`

## CHATGPT_WRITER transport
This execution class does not assume local clone/worktree access. When local Git is unavailable, use the connected GitHub API transaction:

`READ exact branch HEAD -> fetch exact files -> create blobs -> create tree from exact parent tree -> create commit with exact parent -> fast-forward this branch only -> re-read exact branch HEAD/diff`.

Never update `main`, force-push over unexpected branch movement, edit `cep-writer/` task authority as the Writer, or infer executable PASS from source inspection. Build/test/browser/visual claims require an actual runner.

## C1 objective
Close only the shared systemic defects proven by Controller F-001..F-041:
1. remove duplicated global `Node.prototype.closest` / `Window.prototype.closest` mutation while preserving narrow EventTarget normalization at existing event seams;
2. stop Library donor semantic toolbar/content from being the implicit universal semantic carrier;
3. preserve one shared toolbar grammar and small universal action set while binding each Surface's own commands;
4. remove cross-Surface semantic dependence on generic `surface.mode='read'`; retain Read/Edit only where a compatible Structured editor/document actually owns that state;
5. bind Profile-defined LEFT/RIGHT/BOTTOM roles through existing shared pane/bottom hosts rather than collapsing them into CENTER or leaving donor Library payload;
6. preserve the already-proven responsive pane mechanics, including targeted 1024 RIGHT reveal = `22/22 PASS`;
7. preserve Library and Learn donor-compatible value through explicit compatible binding rather than consumer-specific masks;
8. add negative full-carrier falsification proving foreign Library Read/Edit/Save/KU/context semantics are absent on incompatible Surfaces.

C1 MUST NOT fix W04 seeded data, Today provider truth, RQ acceptance data, Visualize provider/profile mismatch, W03 synthetic domain objects, or Product database/provider semantics.

## Evidence baseline
Controller full-carrier run `35670265046`, artifact `10670544348`, digest `sha256:bd113da504671fa8929f4c70290cbd5e3c0227b005aa5cdf546999178a52b210`: Node 22.16.0, model 210/210, genuine localhost, 46 fresh screenshots, donor Read/Edit + Save leak on 18 incompatible Surfaces / 36 captures.

Targeted run `35671028123`, artifact `10671850032`, digest `sha256:50800de8905b75bcc85b78e2074965bf4790a47a30a91c04e1319ff897c10133`: responsive RIGHT 22/22 PASS; Runs causal shutdown Product path clean; three Visualize legacy relation browser failures are stale assumptions against the current read-only acceptance provider while the independent Visualize Profile/provider mismatch remains open for B3.

## Stop gate
Stop instead of widening scope if correction needs a new semantic owner/dependency, domain/provider mutation, persistence/schema/runtime truth changes, loss of Library/Learn donor value, or consumer-local CSS hiding as the primary shared fix.
