# CEP CORR02 C3 — Today Provider / Epistemic / Continuation Truth

**Writer class:** `CHATGPT_WRITER`  
**Mode:** `MUTATING CANDIDATE_ONLY__LOCAL_FIRST__NO_SELF_PROMOTION__NO_MAIN_MERGE__NO_RELEASE`  
**Exact accepted Product parent:** `1acf9d691b27b1a271f149139e055110971b1fa0`  
**Accepted Product source:** `760578213b3f3e352dd2b08dd05fab183c3ffad9625844a1d0ea82e30cc9e454 / 273 files`  
**Transport branch:** `capsule/corr02-c3-today-truth`

## Objective
Close only `F-007..F-011`, `F-044`, plus Today-specific provider/data/continuation truth required by those findings. `F-006` is root-context evidence for the current acceptance-provider binding and must be removed from normal Product truth where needed, but do not widen into persistence/database/provider architecture.

Affected Surface:
- Today

Consume accepted C1 shared carrier/regions/contextual-toolbar mechanics and accepted C2 truth boundaries. Do not duplicate or rewrite Foundation/W04 owners.

## Required truth corrections
1. Normal Today Product composition must not bind `LOCAL_ACCEPTANCE_PROJECTION_ONLY` data as current user truth. Acceptance/demo providers may remain explicit fixtures/tests only.
2. If no admitted current Today provider exists, normal Product must render truthful `UNAVAILABLE` / `AVAILABLE_EMPTY` / `PARTIAL` / `STALE` / `ERROR` according to the current Today SurfaceProfile; do not fabricate sessions, progress, recommendations or attention items.
3. Resume/continuation availability must require exact current target-resolution/provider proof. `exists:true`, `readable:true`, or fixture `targetState:'RESOLVABLE'` assertions alone are not sufficient when not sourced from an admitted current resolver.
4. Resume remains a handoff only: no Today canonical progress/Mastery/access writes; `mutated:false`, no access decision ownership.
5. Recommendation source/version/reason/rationale must be provider-observed and exact-version bound. `today.why` stays unavailable if exact source/version/rationale truth is unresolved.
6. Reconcile Today Product state vocabulary with the packaged profile. Do not silently mutate authority; if the current profile cannot be satisfied without authority contradiction, STOP rather than invent a new state contract.
7. On provider failure, preserve the last successful projection as stale/retained context with a safe correlation/error reference where the profile requires it; do not erase it by overwriting `lastProjection` with a failed empty projection.
8. Source-specific stale/unavailable/error truth must remain visible. Preserve provider identity, state and `observedAt` where available; do not collapse all providers into a single misleading aggregate.
9. Today remains orchestration/command projection only. Progress is projected, Mastery is W04-owned, recommendation never grants/denies access.
10. Preserve typed LEFT/CENTER/RIGHT/BOTTOM/TOOLBAR region binding from accepted C1; C3 may correct Today-specific semantic content in those regions but may not replace shared pane mechanics.

## Writable Product allowlist
- `stack/native-typescript/adapters/today/**`
- `stack/native-typescript/surfaces/today/**`
- `stack/native-typescript/surfaces/m0-controller-composition.ts`
- bounded C3-only tests/harness under `tools/c3-*/**`
- bounded handoff under `writer-output/presentation-corr02-c3/**`

Everything else is READ ONLY unless a directly reproduced prerequisite correctness defect satisfies OD-070 and requires Controller adjudication before scope widening.

## Explicitly prohibited
- Foundation/shared carrier/toolbar/region owner rewrites;
- W04 Evidence/Reviews/Mastery/Portfolio changes;
- RQ/Visualize/W03/W05 corrections;
- SQLite/schema/persistence/runtime/terminal architecture;
- Product dependency/package changes;
- fabricating a real Today provider, target resolver, canonical progress, Mastery, recommendation authority or access decision;
- changing acceptance fixtures merely to make normal Product look current unless their explicit fixture classification remains intact;
- `cep-writer/**` mutation by the Writer;
- main merge/push, release, deployment, self-promotion.

## Required local-first execution
Use the Capsule as the only inbound workspace. Start from the prebuilt Today Visual Bootstrap:
`verify -> materialize -> inspect current Today baseline/reference mapping -> compare -> diagnose -> fix -> local build/test/browser -> recapture -> compare -> repeat`.

GitHub Actions is not the development loop. Use the packaged C3 harness locally. Managed execution is final corroboration only when a proof class materially requires it.

## Positive / negative falsification gates
- normal Product Today contains no current-looking Balanced6 acceptance session/recommendation/progress truth;
- provider-unbound Today is truthful and usable, not a fabricated empty success;
- fixture continuation assertions cannot enable Resume in normal Product without admitted target resolution;
- exact continuation target missing/removed/forbidden/unavailable states disable Resume truthfully;
- `today.resume` never mutates progress/Mastery/access truth;
- recommendation `why` requires exact selected recommendation version + source-backed rationale;
- provider error preserves required last-success/stale context and records failure truth;
- stale/unavailable providers expose provider-specific state and `observedAt` when available;
- packaged profile state vocabulary is honored or any contradiction is reported, not silently normalized;
- Today LEFT/CENTER/RIGHT/BOTTOM/TOOLBAR remain typed and no Library/W04 semantic leakage appears;
- no dependency drift; `npm test`, `npm run build:runtime`, `npm run runtime:check` remain clean;
- rerun applicable Today browser/visual states at `1440×1000` and `1024×900`;
- inspect every material final screenshot against `CEP_TODAY_MAIN_ORCHESTRATION_REFERENCE.png` as Presentation authority only.

## Required final handoff
Report exact parent, candidate HEAD/tree, Product source identity, changed Product paths, finding-by-finding disposition, provider/continuation/recommendation/error-state falsification, test results, visual disposition, any stale-harness/evidence debt, and final artifact identities.

## Stop Gate
End exactly:
`C3_CANDIDATE_ONLY__CONTROLLER_AUDIT_REQUIRED`
