# CEP Writer Baseline — START HERE

This `main` branch is the Controller-bound Writer baseline, not an active mutation mission.

A Writer mutates only a Controller-bound mission branch created from one exact `main` commit. Read the common baseline authority and references here; the exact mission/task overlay belongs only to the mission branch.

Common reads: `WRITER_AUTHORITY_BASELINE.md`, `authority/WRITER_OWNER_DECISIONS.csv`, `WRITER_INPUT_MANIFEST.json`, `REPOSITORY_BOUNDARY.md`, `GIT_WORKFLOW.md`, `references/`, `profiles/`, `contracts/` and applicable Product/tooling paths.

Do not mutate `main` as a Writer. GitHub is the self-contained execution plane; Google Drive remains the live Controller/custody plane.
