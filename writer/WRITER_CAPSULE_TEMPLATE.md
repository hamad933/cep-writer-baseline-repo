# Surface Writer capsule

Fill the named fields; consume the existing modules.

- Surface/profile: `profiles/<surface>.json`
- Exact input candidate hash: `<manifest sha256>`
- Current authority: `authority/OWNER_CURRENT_RULES.json` plus source/conflict ledgers.
- Domain object identity and immutable source revision: `<refs>`
- CENTER family engine: `<profile.family_engines>`
- TOP/LEFT/RIGHT/BOTTOM payload bindings: `<domain content only>`
- Domain commands: `<id, owner, availability, effect, focus, invocation identity>`
- Adapter capabilities: `<real supported capabilities, unsupported reasons>`
- Required behavior proof: `<before → pointer/keyboard action → single owner → after → visible effect → negative oracle>`
- Source requirements addressed: `<exact ids>`
- Remaining requirements: `<exact ids and reasons>`

Do not duplicate pane geometry, toolbar placement, context menu, command palette, settings, focus, responsive rules, editor, spatial input or session presentation. Change a shared owner for a shared mechanic, run affected consumers and attach candidate-bound evidence. Do not silently convert OPTIONAL into a new engine. Do not infer accepted design from a version/name. Do not import historical auth/server/deployment conventions.

Writer output: bounded patch; source/command/profile delta; proof receipt; screenshots; unresolved deviations. Status stays NOT_OWNER_ACCEPTED. A passing test is not Owner adjudication.
