# CEP Foundation — Master Work Resume Delta v2.0

## 0. Purpose

This file is a **delta**, not a replacement for the live state of the paused Work conversation.

The Work mission must continue from its exact current checkpoint and preserve valid completed work. This Controller had access to the verified v0.2 baseline and the v0.2.1 Starter Pack, but it was not supplied an exact exported paused-Work checkpoint.

Therefore:

`EXISTING_WORK_CHAT_LIVE_STATE > CONTROLLER_ASSUMPTION`

Before changing code, the existing Work conversation must classify each task as:

- `ALREADY_COMPLETED_IN_WORK`
- `IN_PROGRESS_AT_PAUSE`
- `NOT_STARTED_FROM_EXISTING_MISSION`
- `NEWLY_ADMITTED_BY_THIS_CONTROLLER`

Do not redo a completed proof unless a newly applied change invalidates it.

## 1. Hard authority corrections

### Current authority

1. Direct current Owner decisions.
2. Current Owner corrections.
3. Current canonical/control artifacts.
4. Accepted executable donors.
5. Current Foundation lineage.
6. Current audit evidence.
7. Chat A/B only after this Controller's corrections.

### Excluded authority

Do not use old GitHub, old Production, historical stack architecture, or W03 v3.4 implementation as current Foundation truth.

W03 authority is exactly:

`W03_V3_4_SAME_LINEAGE_VALUE_DOMAIN_REQUIREMENT_EVIDENCE_DONOR_NOT_AUTHORITY`

The old token and semantic aliases must not survive.

## 2. Architecture law

`GLOBAL EXECUTABLE FOUNDATION`
→ `SURFACE-FAMILY ENGINE`
→ `THIN DOMAIN ADAPTER`
→ `SURFACE COMPOSITION`

Use the narrowest correct reusable owner.

The active ownership layers are:

- `GLOBAL_FOUNDATION`
- `STRUCTURED_FAMILY`
- `SPATIAL_FAMILY`
- `OPERATIONAL_FAMILY`
- `ANALYTICAL_FAMILY`
- `REVIEW_AUDIT_FAMILY`
- `DOMAIN_SHARED`
- `DOMAIN_ADAPTER`
- `SURFACE_SPECIFIC`

## 3. New Controller delta relative to original v0.2.1 mission

### Delta A — Deferred governance supersession

Original v0.2.1 delivery/status contains `zero-loss deferred ledger` and `DEFERRED_WORK_ZERO_LOSS`.

This is superseded.

The successor must maintain two concepts:

1. active valuable deferred work;
2. dropped/superseded/duplicate/historical traceability.

Only the first enters future Writer obligation.

### Delta B — One bounded Global host extraction

Chat B's C02/C05/C06/C10 are not separate architecture projects.

Treat them as one Global Foundation host correction:

- `WorkspaceFoundation`
- `PaneLayoutController`
- global transient/focus lifecycle
- shared Context Inspector presentation
- `ContextDescriptorProvider`

The goal is to remove the need for representative non-Library consumers to unconditionally bootstrap the accepted Library runtime **after** required owners exist.

Do not broadly rewrite donor CSS or final Shell.

### Delta C — Writer governance source-scope cleanup

Future Writer scaffold/ownership/versioning is admitted only after owners are corrected.

It must not require:

- GitHub branch/SHA;
- old Production mapping;
- historical stack;
- W03 implementation authority.

It must generate current Foundation inheritance and only valuable deferred work.

## 4. Admitted work

Exactly the following Controller tasks are admitted. If the live Work state has already completed a task correctly, retain it and satisfy only the new or invalidated gate.

## FND-RSM-P0-001 — DO_NOW_P0

**Source requirements:** OWNER-W03-AUTH-CURRENT; C/F001; B/C03; A/W03 authority atoms

**Why high value:** Active generators can reproduce a false W03 implementation-authority rule; this contaminates every future Writer intake.

**Current state:** PRESENT_BUT_WRONG: token/semantic alias exists in generated authority artifacts and generators.

**Current owner:** authority generators + generated registries/handoff artifacts

**Target owner:** Canonical authority registry/generators with W03 donor-only disposition

**Ownership layer:** `GLOBAL_FOUNDATION / WRITER_GOVERNANCE`

**Affected family:** ALL

**Affected surfaces/consumers:** All future consumers of Foundation authority

**Expected areas:** tools/extract-w03-v34.py; tools/build-writer-contracts.py; tools/finalize-candidate.py; authority/*; contracts/FOUNDATION_RUNTIME_REGISTRY.json; FINAL_HANDOFF_AR.md; architecture prose that carries semantic alias

**Required change:** Replace wrong W03 baseline authority with W03_V3_4_SAME_LINEAGE_VALUE_DOMAIN_REQUIREMENT_EVIDENCE_DONOR_NOT_AUTHORITY in source generators, regenerate derived artifacts, and disposition each reused W03 item independently.

**Forbidden change:** Do not redesign W03; do not copy W03 UI/layout/component architecture; do not use old Production/GitHub as replacement authority.

**Required adapter:** None; W03 domain requirements may later enter domain adapters only after independent disposition.

**Semantic command owner:** No command change required.

**State owner:** Authority/requirement registry state only.

**Dependencies:** None; run first.

**Automated proof:** Negative scan exact token + semantic aliases = 0 after regeneration; positive assertion exact donor-only authority status present in generator outputs.

**Screenshot:** NO

**Stop condition:** All active generated artifacts and their generators pass negative/positive authority scans.

## FND-RSM-P0-002 — DO_NOW_P0

**Source requirements:** DIRECT-OWNER-DEFERRED-VALUE-FILTER-20260911; C section 12/14 stale zero-loss law

**Why high value:** Prevents every future Writer from carrying obsolete, duplicate and low-value historical work as active obligation.

**Current state:** WRONG_GOVERNANCE: current v0.2 check/mission still encodes deferred.zero_loss / DEFERRED_WORK_ZERO_LOSS.

**Current owner:** Foundation governance registries, checks, handoff/finalization generators

**Target owner:** Value-filtered deferred-work governance contract

**Ownership layer:** `GLOBAL_FOUNDATION / WRITER_GOVERNANCE`

**Affected family:** ALL

**Affected surfaces/consumers:** All future Writer missions

**Expected areas:** deferred/open-work registries; conformance checks; finalize/handoff generators; Writer intake output

**Required change:** Replace zero-loss active backlog semantics with value filtering; emit active high-value deferred entries separately from dropped/superseded/duplicate/historical traceability.

**Forbidden change:** Do not delete provenance; do not turn dropped traceability into future implementation obligation.

**Required adapter:** None

**Semantic command owner:** None

**State owner:** Deferred-work governance registry

**Dependencies:** P0-001 if common generators are regenerated together.

**Automated proof:** Fixture containing high-value, low-value, duplicate, superseded and historical items yields only valuable active backlog and separate exclusion register; obsolete DEFERRED_WORK_ZERO_LOSS status absent.

**Screenshot:** NO

**Stop condition:** Generated handoff/scaffold and npm check enforce value-filtered deferred semantics.

## FND-RSM-P0-003 — DO_NOW_P0

**Source requirements:** C/F002; B/C01; B/ABS-001; B/ABS-002; Owner Structured/editor requirements

**Why high value:** Breaks the most expensive Library-specific dependency and creates the reusable Structured family boundary needed by Library, Learn and future document surfaces.

**Current state:** PARTIAL: accepted-runtime.js contains reusable editor behavior but non-Library composition mounts Library-oriented runtime/state.

**Current owner:** dist/foundation/accepted-runtime.js + Library-oriented runtime state

**Target owner:** StructuredFamilyEngine/StructuredSurfaceHost + StructuredDocumentDomainAdapter; thin LibraryDomainAdapter and LearnDomainAdapter

**Ownership layer:** `STRUCTURED_FAMILY + DOMAIN_ADAPTER`

**Affected family:** STRUCTURED_FAMILY

**Affected surfaces/consumers:** Library; Learn; future structured-document surfaces

**Expected areas:** new/existing structured family module(s); domain adapter contracts; stack/native-typescript composition; donor compatibility seam; conformance tests

**Required change:** Extract minimum durable editor/document mechanics: identity, working revision/state, structured model, schema/capabilities, commit/save, selection/clipboard, undo-redo/history/recovery ownership, domain metadata, command availability, and minimum Notes/source binding seam. Learn must bind its own domain adapter rather than cloning Library fixture/state.

**Forbidden change:** No 330-function rewrite; no Library tree/search/KU semantics in shared engine; no global mutable history core; no final editor redesign.

**Required adapter:** StructuredDocumentDomainAdapter + LibraryDomainAdapter + LearnDomainAdapter

**Semantic command owner:** SemanticCommand registry remains global; Structured family registers document commands; adapter supplies domain availability/capabilities.

**State owner:** Structured family owns reusable document working state; domain adapter owns domain identity/metadata/persistence mapping; history stays family-scoped.

**Dependencies:** P0-001 authority first; preserve donor provenance.

**Automated proof:** Synthetic non-Library adapter mounts/edit/undo-redo/save without Library fixture/search/tree/KU state; Library donor-parity smoke; Learn state independence.

**Screenshot:** YES — one representative structured surface only if visible composition changes

**Stop condition:** A non-Library synthetic/Learn consumer exercises shared Structured mechanics without accepted Library domain state, while Library compatibility remains proven.

## FND-RSM-P1-004 — DO_NOW_P1

**Source requirements:** DIRECT-OWNER-MULTISELECT-LAW; C addendum B; B/C04

**Why high value:** Prevents divergent toolbar logic and makes Connect/destructive actions correct for every Spatial consumer.

**Current state:** PARTIAL: shared Selection Action Surface exists, but availability logic is split/insufficiently canonical.

**Current owner:** selection action surface + command/domain checks in several routes

**Target owner:** ActionAvailabilityResolver (or equivalent single canonical contract)

**Ownership layer:** `GLOBAL_FOUNDATION contract + SPATIAL_FAMILY resolver rules + DOMAIN_ADAPTER capability`

**Affected family:** SPATIAL_FAMILY; potentially other action-surface families

**Affected surfaces/consumers:** Visualize; eligible W03 spatial consumers; future spatial surfaces

**Expected areas:** ActionAvailability contract/resolver; Selection Action Surface; RelationDomainAdapter integration; command preflight

**Required change:** Derive action visibility/enabled state from selection count/types/compatibility/domain capabilities/canonical state/mode/registered command/destructive policy. Connect only for exactly two eligible endpoints unless adapter declares deterministic bulk semantics.

**Forbidden change:** No per-surface Visualize/Enterprise/Scenario/Lab selection toolbars; no implicit pairwise bulk connect.

**Required adapter:** RelationDomainAdapter and domain capability descriptors

**Semantic command owner:** Global SemanticCommand registry; each action resolves to one registered command.

**State owner:** Canonical selection model remains family/domain appropriate; resolver is derived state, not a second canonical store.

**Dependencies:** Preserve RelationInteraction.

**Automated proof:** Availability matrix for 0/1/2/>2 selections, mixed entity types, incompatible endpoints, disabled domain capability, duplicate relation/canonical constraints, destructive policy; UI and command preflight return same result.

**Screenshot:** YES — compact action surface for exactly-two eligible selection

**Stop condition:** One availability result drives presentation and command preflight across at least Visualize and one eligible W03 consumer.

## FND-RSM-P1-005 — DO_NOW_P1

**Source requirements:** B/C02+C05+C06+C10; Owner central-modification law

**Why high value:** One bounded extraction removes the common accepted-runtime host choke point instead of creating four competing abstractions.

**Current state:** PARTIAL_TRANSITIONAL: WorkspaceFoundation facade delegates pane/transient/inspector behavior to donor DOM/runtime; mountAcceptedRuntime is unconditional in current composition.

**Current owner:** accepted-runtime.js + WorkspaceFoundation facade

**Target owner:** WorkspaceFoundation shared host with PaneLayoutController, global transient/focus lifecycle, reusable ContextInspector presentation + ContextDescriptorProvider boundary

**Ownership layer:** `GLOBAL_FOUNDATION`

**Affected family:** STRUCTURED; SPATIAL; OPERATIONAL

**Affected surfaces/consumers:** Representative Library/Learn/Visualize/W03 consumers

**Expected areas:** foundation/workspace host; pane/layout/preferences; context inspector; transient/focus lifecycle; stack bootstrap/composition

**Required change:** Extract only the cross-family host mechanics needed to make representative non-Library consumers cease unconditionally depending on accepted-runtime. Keep preferred vs effective pane state distinct and keep domain context supplied through descriptors.

**Forbidden change:** No broad donor CSS rewrite; no final Shell redesign; no merging family-specific selection/history/session state into global host.

**Required adapter:** ContextDescriptorProvider or equivalent domain/family descriptor boundary

**Semantic command owner:** Global commands for pane/context/focus actions only.

**State owner:** Global workspace layout/transient/focus state; domain-specific context remains outside.

**Dependencies:** P0-003 establishes Structured seam; P1-004 for shared selection action context where relevant.

**Automated proof:** Donor-free host fixture; preferred/effective pane state; transient dismissal; focus restoration; same ContextInspector presentation with at least two descriptor providers; representative non-Library bootstrap has no unconditional accepted-runtime dependency.

**Screenshot:** YES — one pane/context responsive state

**Stop condition:** Shared host mechanics are owned once and at least two families inherit them without Library-domain state.

## FND-RSM-P1-006 — DO_NOW_P1

**Source requirements:** DIRECT-OWNER-INTERNAL-SIM-LAW; C/F003; B/C08

**Why high value:** Keeps Operational family reusable while preserving the already-proven internal simulation path.

**Current state:** PRESENT_BUT_WEAK: generic Operational presentation contains InternalSimulation-specific labels/ownership assumptions.

**Current owner:** Operational shared layer with hardcoded InternalSimulation metadata

**Target owner:** RuntimeProviderDescriptor supplied by RuntimeAdapter; InternalSimulationAdapter remains provider implementation

**Ownership layer:** `OPERATIONAL_FAMILY + DOMAIN/PROVIDER_ADAPTER`

**Affected family:** OPERATIONAL_FAMILY

**Affected surfaces/consumers:** W03 runtime/terminal surfaces and future operational consumers

**Expected areas:** OperationalSurfaceManager; TerminalSurface; RuntimeAdapter contract; provider metadata/capabilities; tests

**Required change:** Move provider label, owner, capabilities and command capability metadata behind RuntimeAdapter/provider descriptor. Preserve stable session identity and truthful live/recorded semantics.

**Forbidden change:** Do not add PTY, PowerShell, WSL, Docker, VM, SSH, remote shell or external provider implementations.

**Required adapter:** RuntimeAdapter + RuntimeProviderDescriptor + InternalSimulationAdapter

**Semantic command owner:** Global SemanticCommand routes operational command to RuntimeAdapter; provider handles supported command execution.

**State owner:** OperationalSessionModel owns session identity; SimulatedDeviceEngine owns canonical simulated device state.

**Dependencies:** Preserve existing causal simulation engine.

**Automated proof:** Provider metadata fixture proves generic UI changes with descriptor; shutdown causal path changes same device state observed by terminal/node/context/telemetry; unsupported provider capability hides/disables command truthfully.

**Screenshot:** YES — one provider-neutral terminal/session presentation if visual label changes

**Stop condition:** No InternalSimulation-specific identity is hardcoded in reusable Operational presentation and causal simulation remains green.

## FND-RSM-P1-007 — DO_NOW_P1

**Source requirements:** C/F007+F008; B/C11+C12; Owner pointer/keyboard laws

**Why high value:** Closes shared Spatial interaction defects before they multiply across graph/topology/canvas consumers.

**Current state:** PARTIAL_NEEDS_EXTENSION

**Current owner:** Spatial interaction engine

**Target owner:** Spatial pointer lifecycle + SemanticSpatialNavigator

**Ownership layer:** `SPATIAL_FAMILY`

**Affected family:** SPATIAL_FAMILY

**Affected surfaces/consumers:** Visualize; W03 graph/topology; future spatial consumers

**Expected areas:** pointer/context lifecycle; keyboard navigation; focus rendering; spatial conformance tests

**Required change:** Terminate Ctrl+RMB suppression on pointerup/pointercancel/missing-contextmenu/abnormal termination; define semantic keyboard minimum separating focus, selection, camera movement and object movement.

**Forbidden change:** Do not build exhaustive graph screen-reader system or surface-specific keyboard forks.

**Required adapter:** Spatial domain focusability/action descriptors where domain semantics differ

**Semantic command owner:** Registered spatial commands for activate/context/edit/escape/relation routes.

**State owner:** Spatial focus state distinct from SelectionModel; camera/model state remain their canonical owners.

**Dependencies:** P1-004 availability contract for context actions.

**Automated proof:** Ctrl+RMB drag suppresses only its own context event then next normal RMB opens actions; pointercancel/abnormal cleanup; directional focus, activation, selection extension, Escape and visible focus independent of selection.

**Screenshot:** NO unless focus-ring appearance materially changes

**Stop condition:** All pointer lifecycle regressions and semantic keyboard minimum pass on shared engine.

## FND-RSM-P1-008 — DO_NOW_P1

**Source requirements:** C/F004+F005; A W03 318 atoms/69 sections; DIRECT_CURRENT_OWNER W03 policy

**Why high value:** Gives future Writers truthful reusable dispositions without converting historical W03 detail into implementation authority or 69 tasks.

**Current state:** PARTIAL: current 69-section disposition has broad OPEN_MISSING and profile semantics remain partial.

**Current owner:** W03 authority/disposition registry + SurfaceProfile scaffold

**Target owner:** Requirement/reuse disposition registry with exact reusable-owner classifications

**Ownership layer:** `WRITER_GOVERNANCE + relevant FAMILY/DOMAIN classifications`

**Affected family:** GLOBAL; SPATIAL; OPERATIONAL; STRUCTURED where applicable

**Affected surfaces/consumers:** W03 Enterprise/Scenarios/Labs/Runs/Results and future consumers of extracted mechanics

**Expected areas:** W03 disposition map; profile schema/rows exposed by changed ownership; Writer intake registry

**Required change:** Compile all 69 sections/318 atoms into reusable mechanic/domain-value groups; close only Foundation-relevant subset as reconciled/partial/deferred/superseded/not-foundation/owner-decision. Correct profile contradictions caused by new owners while keeping SEMANTIC_ADJUDICATION_PARTIAL.

**Forbidden change:** No 69 feature tasks; no W03 layout/design authority; no all-23-surface adjudication.

**Required adapter:** Thin W03 domain adapters only where executable domain semantics are actually needed.

**Semantic command owner:** Reference existing/new canonical command owners, never W03-local duplicates.

**State owner:** Reference canonical owner per mechanic; disposition registry itself owns no runtime state.

**Dependencies:** P0-001, P0-003, P1-004/005/006/007 so dispositions point to final owners.

**Automated proof:** 69 sections present exactly once at section level; 318 source atoms trace to grouped owner/disposition; no atom becomes standalone implementation task by default; changed profile rows validate against schema.

**Screenshot:** NO

**Stop condition:** Foundation-relevant subset has truthful exact disposition and every deferred pointer names future family/surface owner.

## FND-RSM-P1-009 — DO_NOW_P1

**Source requirements:** C/F006; C section 5/10; Owner test economics

**Why high value:** Turns high-value interaction claims into reproducible evidence and reduces repeated manual Writer proof.

**Current state:** MISSING_REPRODUCIBLE_BROWSER_HARNESS; unit/check suites already green on baseline.

**Current owner:** existing test/check tooling + static receipts

**Target owner:** Small deterministic browser/conformance harness

**Ownership layer:** `GLOBAL_FOUNDATION / CONFORMANCE_TEST`

**Affected family:** GLOBAL; SPATIAL; OPERATIONAL; STRUCTURED

**Affected surfaces/consumers:** Representative consumers only

**Expected areas:** test harness; 4–6 critical flow specs; receipts; targeted screenshots

**Required change:** Automate only critical cross-owner flows and pair visual checks with canonical-state/command assertions. Add central-modification proof.

**Forbidden change:** No exhaustive screenshot inventory or permutation explosion; screenshots cannot substitute causal assertions.

**Required adapter:** Use real test adapters/fixtures matching P0/P1 contracts.

**Semantic command owner:** Assert commands route through registry to canonical owners.

**State owner:** Assert canonical stores, do not add test-only product state.

**Dependencies:** Run after behavior-changing P0/P1 tasks.

**Automated proof:** 4–6 browser flows: workspace pane/context; selection→Connect→Composer→edge; relation edit convergence/reuse; operational session→shutdown causal sync; structured donor-free flow or Bidi/preference smoke; plus central modification inheritance.

**Screenshot:** YES — maximum 3–4 targeted visual captures total

**Stop condition:** Harness is deterministic/rerunnable; all flows green; screenshots limited to properties not sufficiently communicated by state assertions.

## FND-RSM-P1-010 — DO_NOW_P1

**Source requirements:** C addendum C/D/E/G; B/C09+C14+C15; DIRECT_CURRENT_OWNER future-Writer system

**Why high value:** Encodes corrected ownership so future Writers consume Foundation instead of rediscovering architecture or copying mechanics.

**Current state:** PARTIAL: ownership/profile/contract registries exist; duplicate guard and scaffold are incomplete.

**Current owner:** Foundation registries/contracts/profile scaffold

**Target owner:** MechanicOwnershipConformanceGuard + lightweight contract versions + deterministic Foundation WriterScaffoldGenerator

**Ownership layer:** `WRITER_GOVERNANCE`

**Affected family:** ALL

**Affected surfaces/consumers:** Future Writer targets

**Expected areas:** ownership registry; duplicate guard; contract metadata; scaffold generator; profile output; conformance tests

**Required change:** After runtime owners are corrected, encode mechanic owner/state owner/command owner/adapters/consumers/deviations; version durable contracts; generate surface intake with Foundation identity, profile, inherited family, adapters, commands, preferences, duplicate prohibitions, valuable deferred items and minimum proof.

**Forbidden change:** No GitHub branch/SHA/old Production mapping prerequisite; no auto-building surfaces; no package-management bureaucracy; no claim profile semantic completeness.

**Required adapter:** Scaffold references required domain adapters; does not implement them.

**Semantic command owner:** Registry metadata only.

**State owner:** Registry metadata only.

**Dependencies:** LAST: P0-001..003 and P1-004..008 must establish correct owners first.

**Automated proof:** Duplicate-owner fixtures flag likely duplicate relation/pane/context/session managers; compatible vs breaking contract version fixture; scaffold smoke for Structured, Spatial and Operational targets contains no GitHub/old Production assumptions and only high-value deferred entries.

**Screenshot:** NO

**Stop condition:** Generated Writer intake is deterministic, current-owner-correct, source-scope-clean and reduces rediscovery without implementing target surface.


## 5. High-value deferred work

Do not implement file `13_HIGH_VALUE_DEFERRED_WORK.csv` now unless a current P0/P1 task requires only a minimal seam explicitly stated in its task row.

Examples deliberately deferred:

- full cross-family NotesCore;
- OperationalSurfaceAttachmentController;
- SessionShelf;
- full LayoutProfileManager;
- full Analytical family;
- full Review/Audit family;
- GuidanceSupportUI;
- epistemic shared contract;
- external runtimes;
- full 23-surface semantic adjudication.

## 6. Intentionally dropped/superseded work

File `14_DROPPED_SUPERSEDED_LOW_VALUE_ITEMS.csv` is exclusion traceability only.

Do not reactivate:

- deferred zero-loss law;
- GitHub/Production mapping prerequisite;
- old stack authority;
- W03 implementation authority;
- all-330 rewrite;
- 69/318 feature conversion;
- all-23 surface build;
- final Shell redesign;
- screenshot inventory;
- stack tournament;
- SaaS/multi-user defaults;
- global mutable history runtime owner;
- StateOwnershipBoundary runtime service;
- unnecessary external runtime/PTY work.

## 7. Test economics

Use file `15_TEST_AND_VISUAL_EVIDENCE_MATRIX.csv` as the minimum evidence map.

Primary evidence:

- state transitions;
- semantic command routing;
- canonical ownership;
- adapter boundaries;
- duplicate-owner guards;
- causal runtime effects;
- shared inheritance;
- reproducible browser flows.

Visual evidence is supporting only.

Target at most `3–4` screenshots across the whole correction wave unless a newly introduced visible regression genuinely requires another one.

## 8. Required end-of-Work package

The existing v0.2.1 mission still targets one successor:

`CEP_FOUNDATION_FORGE_v0.2.1_BOUNDED_FOUNDATION_CORRECTION_CANDIDATE.zip`

Its delivery package should include:

- exact bytes and SHA-256;
- complete manifest;
- changed-file register;
- authority before/after register;
- Structured extraction map;
- W03 disposition update;
- browser conformance receipt;
- automated test receipts;
- known limitations;
- **high-value deferred ledger**;
- **dropped/superseded/low-value traceability register**.

Do not retain the phrase `zero-loss deferred ledger` as an active requirement.

## 9. Mandatory final gates

All must pass after the final code state:

1. W03 exact forbidden token = 0.
2. W03 semantic aliases recreating implementation authority = 0.
3. Correct donor-only W03 authority positively present in generator outputs.
4. Deferred value-filter governance replaces `deferred.zero_loss`.
5. Structured synthetic non-Library adapter can edit/save/history without Library domain state.
6. Library accepted donor behavior remains materially preserved.
7. Learn no longer requires fake Library fixture mutation for shared editor mechanics.
8. ActionAvailability matrix passes and command preflight equals presentation result.
9. Relation create/edit central owner remains reusable across at least two eligible consumers.
10. Representative non-Library consumer boots shared host without unconditional accepted Library runtime dependency.
11. Pane preferred/effective state, transient/focus restoration and ContextDescriptor reuse pass.
12. Operational provider metadata is adapter-derived.
13. Internal simulation causal shutdown remains same-state truthful.
14. Stable session/live-vs-recorded semantics remain correct.
15. Ctrl+RMB lifecycle and Spatial keyboard minimum pass.
16. W03 69/318 traceability maps to reusable owners without becoming a 69/318 task list.
17. Profile scaffold remains `STRONG_STRUCTURAL_INHERITANCE_SCAFFOLD / SEMANTIC_ADJUDICATION_PARTIAL`.
18. Mechanic duplicate guard and lightweight contract version tests pass.
19. Writer scaffold is deterministic and contains no old GitHub/Production/stack assumptions.
20. Existing `npm test` remains green.
21. Existing `npm run check` remains green after replacing stale zero-loss deferred check.
22. Small deterministic browser suite passes.
23. Manifest/hash/integrity verification passes.
24. `STACK_NOT_FROZEN` remains explicit.
25. No self-acceptance.

## 10. Stop condition

Stop after producing the bounded v0.2.1 successor candidate and its evidence.

Do not:

- continue into surface implementation;
- freeze the stack;
- declare Owner acceptance;
- build a future-controller succession package.

Return the candidate for independent review.
