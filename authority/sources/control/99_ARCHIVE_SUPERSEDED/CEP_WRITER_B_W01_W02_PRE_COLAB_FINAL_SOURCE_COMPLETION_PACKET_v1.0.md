# CEP WRITER B — W01/W02 PRE-COLAB FINAL SOURCE COMPLETION PACKET v1.0

@Google Drive  
@GitHub  
@File Library

PROJECT: Cybersecurity Education Platform — CEP  
ROLE: W01 + W02 SAME-LINEAGE PRE-COLAB SOURCE-COMPLETION WRITER

RESPOND IN ARABIC FOR CONTROL / STATUS. Technical identifiers may remain English.

## 0. Sole baseline

Mutate ONLY a preserved working copy of:

`CEP_W01_W02_PRODUCTION_KNOWLEDGE_WORKBENCH_FORGE_v1.1.4_WRITER_B_FINAL_CLOSURE_HARDENED_RESULT.zip`

Expected SHA-256:

`5954e55db93411293cacb28b4082f54cf53fe8fc3950e9e56ad36cf97b82d636`

Expected Writer source-tree identity inside v1.1.4:

`acd14514dddbd5e45aeb2fc25fed22560511d8ca`

Do NOT restart from v1.1.1, v1.1.3, Astra #1, GitHub main, or any older candidate.

## 1. Read first

Search File Library first for exactly:

`CEP_NEXT_PARALLEL_WAVE_CURRENT_CONTROL.txt`

Then read the current Controller state/manifest and these exact new controls completely:

- `CEP_CONTROLLER_ADJUDICATION_W01_W02_PRE_COLAB_SOURCE_COMPLETION_v1.0.md`
- `CEP_W01_W02_PRE_COLAB_SOURCE_COMPLETION_ACCEPTANCE_MATRIX_v1.0.csv`

Also preserve and extend v1.1.4:

- 80-row Traceability Factory/JSON;
- C001-C032 correction ledger;
- explicit gates register;
- v1.1.4 final assurance receipt.

Read the Accepted Library v1.2.17 direct donor where required for range-format behavior. Do not substitute crosswalk text for direct donor mechanics.

## 2. Mission

Reach the **maximum truthful source/semantic completion point before Colab**.

Implement exactly the two newly Controller-authorized families:

A. canonical persistent `Color / Highlight / Underline` range styling for `LIB-026`, `LIB-037`, `W03-LIB-008` under the V4 contract;

B. historical `C024` bounded server-side Library/R&Q search, cursor pagination, summaries, hierarchy subset projection and lazy detail.

Preserve every verified v1.1.4 capability and owner.

## 3. Range-style contract is fixed

Do not redesign the Controller contract.

- same canonical StructuredDocument/lesson block model;
- `CEP_W02_STRUCTURED_CONTENT_V4`;
- optional `inline_marks` persisted inside existing `lesson_revisions.blocks` JSONB;
- Unicode code-point half-open offsets;
- bounded kinds/tokens exactly as adjudicated;
- no hidden persistent HTML;
- no second editor model;
- PHP/TS normalization parity;
- all save/read/history/recovery/undo/redo/render/clipboard paths preserved;
- legacy VS001/VS002 marked revisions become explicit read-only/edit-in-W02 and server mutation is rejected, preventing C002 regression.

Do not add a DB column merely for `inline_marks`; existing JSONB is the persistence owner unless real source evidence proves otherwise.

## 4. C024 contract is fixed

Implement under the existing W02 owners.

Required endpoints:

- `GET /api/cep/v1/library/catalog`
- `GET /api/cep/v1/rq/sources`
- `GET /api/cep/v1/rq/sources/{source}`

Use bounded cursor pagination: `limit` default 50, max 100; opaque deterministic cursor; canonical ID tie-break; explicit invalid-cursor response.

The Library workspace may bootstrap active exact detail + first bounded page, but must stop materializing the whole corpus. Hierarchy is projected only for loaded page IDs plus authoritative path context and must expose bounded-subset truth.

R&Q list returns summaries; exact source detail is lazy. Active claim analysis queries claim-related sources, not all SourceRecord rows.

Frontends become server-driven for search/paging. Do not keep a full preloaded corpus and filter it locally.

## 5. Required assurance before return

Run every available source/static/semantic/type test and add targeted tests from the supplied acceptance matrix.

Minimum required source-level proofs:

- V4 PHP/TS range golden vectors;
- marked/unmarked VS001/VS002 compatibility tests;
- selection/edit/history/clipboard range tests;
- 251+ Library pagination fixture;
- 251+ R&Q source pagination fixture;
- server ar/en/id search;
- cursor no-duplicate/no-loss and invalid-cursor tests;
- R&Q claim-related analysis source-scope proof;
- frontend server paging/stale-response semantic tests;
- unchanged v1.1.4 regression suite;
- refreshed 80-row Factory + C001-C032 ledger + gates.

A PASS requires material state/data transition, not function/handler presence.

## 6. Gates that MUST remain truthful

Do NOT invent source implementations for:

- durable Linked Notes server persistence without Owner/Controller authority;
- OS cross-app topmost;
- C027 Today provider;
- C028 durable R&Q reconciliation persistence;
- C029 advanced Visualize canonical/durable mutation;
- C030 provider-backed Learn practice/assessment/progression.

Keep C025/C026 runtime/browser evidence separate.

## 7. Colab prohibition for this wave

Do NOT create a new Colab notebook. Do NOT ask the Owner to run Colab during source correction.

The purpose of this wave is to eliminate every remaining safely-resolvable source/contract gap first. After your return, Controller independently reviews the successor and only then prepares reusable cells for the Owner's existing Colab notebook if the remaining gates are genuinely external/runtime/browser/DB only.

## 8. Deliverable

Produce ONE same-lineage successor candidate, suggested:

`CEP_W01_W02_PRODUCTION_KNOWLEDGE_WORKBENCH_FORGE_v1.1.5_PRE_COLAB_SOURCE_COMPLETE_CANDIDATE.zip`

Also return:

- refreshed 80-row Factory XLSX + JSON/CSV;
- updated C001-C032 ledger with C024 source implementation status;
- completed pre-Colab acceptance matrix;
- explicit remaining-gates register;
- source manifest + SHA-256;
- changed-file scope audit;
- test/semantic receipt;
- Arabic Controller handoff.

Desired truthful terminal state if proven:

`PRE_COLAB_SOURCE_CONTRACT_EXHAUSTED / ZERO_ORPHAN_MAPPING_PRESERVED / NO_SOURCE_MISSING_OR_PARTIAL_EXCEPT_GENUINE_AUTHORITY_PROVIDER_PLATFORM_GATE / RUNTIME_BROWSER_DB_EVIDENCE_PENDING`

## 9. Hard stop

NO GitHub/governed-Drive product mutation.  
NO merge/release/deploy.  
NO self-acceptance.  
NO claim of runtime/browser/DB proof from static tests.
