# CEP WRITER A — W03 v3.3 CONTROLLER-BOUNDED CORRECTION PACKET v1.0

PROJECT: Cybersecurity Education Platform — CEP  
WORKSPACE: W03 — Simulation & Enterprise  
ROLE: SAME-LINEAGE BOUNDED CORRECTION WRITER  
AUTHORITY: CONTROLLER CORRECTION DISPATCH  
BASELINE: `CEP_W03_UNIFIED_EXECUTABLE_BLUEPRINT_SYSTEM_v3.3_CUMULATIVE_COMPLETION_CANDIDATE.zip`  
BASELINE SHA-256: `8cd794c99a3e9cd1b6283a2b3cd2f2f2a7161e1921901d8e7162097392690c05`

## READ FIRST

Search File Library for:
1. `CEP_NEXT_PARALLEL_WAVE_CURRENT_CONTROL.txt`
2. `CEP_GLOBAL_OWNER_WRITER_RULES_v1.0.md`
3. `CEP_CONTROLLER_INDEPENDENT_REVIEW_W03_v3.3_v1.0.md`
4. this packet.

Do not ask the Owner to re-upload files available in File Library.

## HARD SCOPE

Mutate a preserved working copy of v3.3 ONLY.

Close exactly:
- `W03-CTRL-001`
- `W03-CTRL-002`
- `W03-CTRL-003`
- `W03-CTRL-004`
- `W03-CTRL-005`

Preserve all five surfaces:
Enterprise / Scenarios / Labs / Runs / Results.

Preserve:
- D001-D026;
- 122 Library donor reconciliation rows;
- 22 Visualize/Spatial rows;
- 44 interaction rows;
- 16 Operational rows;
- 295 provisional traceability rows;
- 66 current-blueprint binding rows;
- shared pointer contract;
- Notes/Context/Operational behavior already present;
- no Production mutation.

## REQUIRED CORRECTIONS

### 001 Guidance
One shared rule:
- default optional guidance hidden;
- empty optional guidance host remains hidden;
- Owner can explicitly show/hide/reopen/dock/move guidance where content exists;
- mandatory safety/error/authority/provider/platform disclosures remain visible and are never moved into optional guidance.

### 002 Internal spatial badges
Remove permanent normal-chrome exposure of donor/implementation labels such as `SHARED SPATIAL CORE ACTIVE`.
Preserve the domain truth through contextual/action-level disclosure.
Do not remove genuine safety/authority disclosures.

### 003 Scenarios stale validation
`Validate` must not expose the old `Spatial authoring dependency مفتوح` warning.
The current validation truth must state shared generic spatial mechanics are active while canonical Scenario relation mutation remains domain-owned.

### 004 Page identity
Update the five current document `<title>` values from v3.2 to current v3.3 successor identity.
Do not rename historical route filenames unless separately authorized.

### 005 Localization
All shared visible Sticky Note/Context transient labels must use the locale owner.
At minimum remove hardcoded visible `Context` and hardcoded group-heading residues where Arabic UI is selected.

## ASSURANCE

Rerun:
- 18/18 logic;
- 26/26 D001-D026;
- JS syntax;
- static/semantic suite;
- five-surface Chromium fallback functional checks;
- 1440 / ~1024 / 390 responsive visual matrix.

Add targeted assertions:
1. default guidance hidden on all 5 surfaces;
2. no visible empty guidance panel;
3. no permanent `SHARED SPATIAL CORE ACTIVE` / internal donor badge in default normal chrome;
4. Scenarios Validate does not show stale open-dependency text;
5. all five `<title>` values current;
6. Arabic Sticky Note/Context transient controls have localized labels.

## SCREENSHOT RULE

If exact route remains `ERR_BLOCKED_BY_ADMINISTRATOR`:
- record exact-route as blocked;
- DO NOT stop screenshots;
- use exact corrected candidate bytes with controlled Chromium `set_content`;
- produce fresh candidate-bound visual evidence;
- classify it `VISUAL_ONLY / CANDIDATE_BOUND`, never exact-route proof.

## STOP

Return one cumulative corrected successor candidate.
Do NOT self-accept.
Do NOT start Blueprint→Production Traceability Factory.
Do NOT implement Production.
Do NOT mutate GitHub or governed Drive product state.
