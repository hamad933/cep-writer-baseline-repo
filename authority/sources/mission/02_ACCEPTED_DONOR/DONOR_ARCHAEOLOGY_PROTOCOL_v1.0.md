# ACCEPTED LIBRARY DONOR — ZERO-LOSS ARCHAEOLOGY PROTOCOL

The accepted donor is an executable oracle for compatible generic presentation/interaction mechanics.

## Identity hard gate
Expected:
- filename: `CEP_LIBRARY_EDITOR_EXECUTABLE_BLUEPRINT_v1.2.17_ACCEPTED_DESIGN_REFERENCE.html`
- bytes: `672893`
- SHA-256: `ea66b58ef122bf2f8ca23fd0aa9e461b07da11ea7390c451902e4b1592d396fd`

Stop on mismatch.

## Required census/disposition
Baseline census supplied by prior deep scan:
- 1,892 lines
- 330 named / unique functions
- 19 data-component families
- 71 CSS variables
- 360 CSS classes
- 176 IDs
- 124 data-* attributes
- 42 media-query occurrences
- 34 addEventListener call sites
- 23 event types

Create an exhaustive coverage ledger. Each named function MUST have one row. Each data-component family MUST have one row. Consolidate CSS/DOM/event primitives into traceable mechanic families without losing source-line pointers.

## Per-mechanic record
Capture:
- source lines/selectors/functions;
- visual/geometry contract;
- DOM/ARIA contract;
- state owner/state keys;
- pointer/keyboard routes;
- command owner;
- focus lifecycle;
- open/close/dismiss/reopen lifecycle;
- responsive behavior;
- customization/persistence;
- Bidi/a11y;
- dependencies on other mechanics;
- donor defect/limitation;
- current Owner supersession;
- foundation/family/domain disposition;
- proof/tests required.

## Value inheritance law
`PRESERVE VALUE, NOT DEFECTS`.
A small Owner correction (e.g. broader dismissal routes) modifies only the affected mechanic; it does not invalidate the rest of the donor.

## No abstraction without parity
An extracted implementation is not accepted because it is elegant. It must preserve the donor's intended composition and behavior, except explicit Owner supersessions, and must pass fresh interaction/visual evidence.
