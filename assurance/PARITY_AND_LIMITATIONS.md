# تفسير الإثبات وحدوده

- baseline الأصلي: `4,062,681` bytes؛ SHA-256 `e9ad5b06110312d23b3002acd4cb345fcfcb583677e83e7b0bb4ac29d516160e`؛ تحقق قبل التعديل.
- Controller v2.0: `122,721` bytes؛ SHA-256 `0bbb013cc767d9d7047a5a5f24fe67865f0ee4e75f9522978e6bd2ceafee406b`؛ guidance/evidence للقراءة فقط.
- متبرع Library المقبول: `672,893` bytes؛ SHA-256 `ea66b58ef122bf2f8ca23fd0aa9e461b07da11ea7390c451902e4b1592d396fd`؛ محفوظ byte-for-byte.
- W03 v3.4: SHA-256 `23b66696a9cc72cb343c74e70f380ccb248e1fbc378d8ce7839ba74111a3d716`؛ يمد خرائط القيمة/المجال/المتطلبات فقط.
- اختبارات النموذج تثبت انتقالات الحالة والعقود. فحوص العقود تثبت الهوية والبناء والملكية والعزل والتصريف والحتمية. إيصال المتصفح يثبت ستة flows تنفيذية مستقلة؛ لا يستبدل أحد هذه الأنواع الآخر.
- Library وحدها تستخدم accepted-runtime كمسار توافق. Learn وVisualize وEnterprise وRuns وGolden تستخدم `WorkspaceFoundationHost` دون mount غير مشروط للمتبرع.
- Runs محاكاة حتمية داخل المتصفح وليست shell خارجيًا. الأمر `shutdown` يتحول إلى `device.shutdown` ويعدل الحالة الكانونية وإسقاطاتها.
- خريطة W03 تحفظ 69 قسمًا و318 atom ضمن مجموعات ملاك/تصريف، مع `SEMANTIC_ADJUDICATION_PARTIAL`; لا تدعي إكمال أسطح W03.
- ستة proof consumers فقط قابلة للتنفيذ. ملفات profile للأسطح الـ23 هي عقود تركيب وليست منتجات مكتملة.
- اختيار المكدس توصية غير مجمدة. لا يوجد ادعاء بقبول المالك أو النشر أو الإصدار أو الجاهزية الإنتاجية.
