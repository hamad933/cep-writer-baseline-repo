# Known limitations — v0.2.1c Controller-Review-Corrected Candidate

- `SEMANTIC_ADJUDICATION_PARTIAL` remains intentional. The 69/318 W03 map is a bounded advisory requirement→owner compilation, not accepted W03 design/implementation authority. Twenty-four independently confirmed high-risk misroutes were corrected; future family/surface Writers re-adjudicate only their relevant subset.
- `WORK_ADMISSION` / `ADMITTED_TASK_ID` columns in the source 318-row CSV are retained only as historical Controller provenance. They MUST NOT be used as current backlog selectors. Current active deferred truth is `assurance/HIGH_VALUE_DEFERRED_LEDGER.json`.
- `ActionAvailability@1.2.1` is a bounded generic policy seam, not a catalog of every future action. Explicitly declared domain capabilities are deny-by-default unless the active domain declares them `true`.
- Browser lineage evidence remains `6/6 PASS` from v0.2.1a. The current independent review environment could not install Node Playwright from offline cache, so v0.2.1c was not falsely claimed as a fresh full browser rerun. The changed nonvisual ActionAvailability branch is independently covered by model/contract/parity tests.
- Full 23-Surface semantic adjudication remains deferred; the inheritance matrix is a strong structural scaffold, not final semantic truth.
- Final Shell/navigation visual composition remains reopened.
- `STACK_NOT_FROZEN`; current native ES-module/erasable-TypeScript proof does not constitute final stack selection or strict TypeScript proof.
- Persistence/backend/desktop wrapper/external runtime providers remain unadmitted until concrete need justifies them.
- Owner acceptance is not implied by this candidate or by Controller succession.

- Browser evidence provenance is now explicit: inherited `6/6` is from `v0.2.1a`; no fresh `v0.2.1c` 6/6 run is claimed.
