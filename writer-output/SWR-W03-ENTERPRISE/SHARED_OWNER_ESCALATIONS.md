# SWR-W03-ENTERPRISE Shared-Owner Escalations

Status: `SHARED_OWNER_ESCALATION_REQUIRED`

## 1. `CBF-003` — shared BottomDeepWorkOwner composition

The governed current finding remains outside Enterprise-local writable ownership. Enterprise keeps only temporary deep-work content and does not create an independent pane owner or per-Surface workaround. The shared owner/final composition must resolve provider ownership centrally.

## 2. `PVF-001` — specialized Enterprise presentation not mounted in current Product integration

The Enterprise-local specialized presentation is corrected and falsified locally, but mounting it into the live Product requires W03 group composition / final wiring paths that are explicitly read-only in this capsule. No `main.ts`, W03 composition, or final-wiring mutation was made. Controller/shared owner action is required to mount the candidate and then obtain genuine-route evidence.

## 3. Shared `SpatialPresentationOwner` sizing defect exposed by Enterprise

Fresh current-candidate browser evidence shows the Enterprise host at approximately `930x813`, while the shared `.spatial-canvas` remains the browser SVG default `300x150`. The shared owner-injected style contains no canvas width/height rule. The shared `.minimap` is also left `position: static` with no governed size/position rule and expands inline to approximately `930x632`. With the existing shared `fit()` mechanic, this forces a `34%` camera readout and a materially undersized topology while the minimap consumes primary-workspace space.

This defect is owned by shared Spatial presentation, not Enterprise. The candidate does not add Enterprise-local CSS to resize or reposition shared Spatial chrome. A central correction must prove propagation to genuine consumers, including Visualize, without W02 local Surface edits, as required by the capsule.

Direct evidence is included in Drive as `spatial_shared_owner_measurement.json` plus inspected topology captures.
